# 项目级 Agent 工作约定

本文件适用于整个仓库。它只保存需要自动生效的稳定规则；项目事实、架构和阶段状态分别由 `docs/` 下的对应文档维护。

## 新会话启动顺序

开始工作时按以下顺序恢复上下文，不要先遍历整个仓库：

1. 运行 `git status --short --branch`，确认分支、未提交修改和工作基线。
2. 阅读 `docs/PROJECT_CONTEXT.md`，确认目标、非目标、约束、假设和未验证事项。
3. 阅读 `docs/ARCHITECTURE.md`，确认当前有效架构和禁止破坏的边界。
4. 阅读 `docs/decisions/README.md`，只打开与当前任务相关且状态有效的 ADR。
5. 阅读 `docs/STATUS.md`，再读取其中指向的当前 Phase 和最近一份相关阶段报告。
6. 最后只检查当前 Phase 涉及的代码、测试、配置和必要历史。

小型局部任务可缩短检查，但不能跳过与改动直接相关的架构边界、ADR 或工作区状态。只有证据不足时才回溯更早报告和 Git 历史。

## 文档职责与事实纪律

- `docs/PROJECT_CONTEXT.md`：产品目标和约束；明确区分已确认事实、当前假设、尚未验证事项。
- `docs/ARCHITECTURE.md`：当前系统是什么；架构变化时直接更新当前描述，不在此堆叠讨论历史。
- `docs/decisions/`：具有长期影响的架构选择、代价和重评条件。
- `docs/STATUS.md`：唯一的当前阶段入口、最近完成项和下一步候选。
- `docs/DELIVERY_WORKFLOW.md`：Phase、验收、失败处理、升级咨询和阶段报告规范。
- `docs/phase-reports/`：已完成正式 Phase 的简短结果证据，不复制完整 diff。

不得把目录名、临时讨论、未验证推测或计划中的设计写成已确认事实。发现文档与代码不一致时，以可复现的代码、测试、日志和接口证据判定现状，并在同一任务内修正文档。

## 默认工作模式

- 简单修复：`Inspect → Fix → Test`。
- 正式 Phase：`Inspect → Plan → Implement → Test → Review → Report → Commit`。
- 重大架构变化：`Inspect → Analyze → Architecture Decision → Implement`。

根据复杂度调整形式，不为小改动制造冗长计划。能从现有架构、ADR、代码和测试推导出的实现细节由 Agent 自主决定，包括文件归属、函数拆分、测试组织、命名、局部错误处理和小范围重构。

默认完成整个闭环：检查上下文、实现、补充或更新测试、调试、回归检查、同步必要文档、按验收标准验证，并在工作区范围干净且验收通过后提交。不要仅因普通实现选择暂停询问用户。

## Phase 纪律

- 同一时间只推进一个未验收的正式 Phase；开始前在 `docs/STATUS.md` 写明目标、非目标、预计模块、验收标准、风险和回滚点。
- Phase 必须单一目标、范围清晰、可测试、可回滚。不要在同一 Phase 混合大规模重构、新功能、框架迁移、性能优化和 UI 改版。
- 范围过大时主动拆为 `Phase 3A`、`Phase 3A.1`、`Phase 3B` 等可独立验收的子阶段。
- 当前 Phase 只实现已确认需要的能力。已经验收的代码默认是稳定资产；除非它阻塞需求或已有可靠性、性能、维护成本、架构违规证据，否则优先增量修改。
- 正式 Phase 完成条件和报告格式以 `docs/DELIVERY_WORKFLOW.md` 为准。验收失败时不得宣称完成或提交“完成态”报告。

## 架构升级边界

只有以下情况需要暂停实现并整理架构咨询包：

- 改变核心服务、模块职责、客户端/服务端边界或主要数据流；
- 实现会使有效 ADR 失效；
- 存在多个长期影响显著、无法由现有约束排除的方案；
- 缺少无法从仓库推断且会改变产品行为的需求；
- 硬件、官方接口或外部系统实测与设计假设冲突；
- 同一核心问题连续两轮实质性修复仍失败。

升级时使用 `docs/DELIVERY_WORKFLOW.md` 中的“架构咨询包”，必须给出证据、已尝试方案、候选方案、当前倾向和最小决策问题。不得用“这里怎么办”代替分析。

## 测试、调试与验收

- Bug 处理顺序：重现 → 收集证据 → 定位根因 → 添加能暴露问题的测试 → 修复 → 回归验证。
- 优先使用自动测试、可重复命令、明确输入/输出、日志和可观测结果；“看起来没问题”不构成验收。
- 不得删除或弱化测试来通过检查，不得隐藏异常，不得用无依据的 `sleep`、吞异常或无限重试掩盖根因。
- 必须采用 workaround 时，记录原因、影响和可移除条件。
- 阶段结束前检查目标、原有测试、回归、复杂度、调试残留、文档一致性和未记录假设。

## Agent 协作

- 默认由单一 Agent 完成工作，避免重复加载上下文。
- 只有任务独立、边界清楚且并行收益高于协调成本时才使用子 Agent；如指定模型，先确认该模型实际可用，禁止静默替换。
- 委派时只传递完成子任务所需的文件范围、证据、约束和输出契约。每个可写文件在同一时段只有一个负责人。
- 适合委派：边界明确的只读调查、独立测试、文档核对或互不重叠模块。不要让多个 Agent 重复扫描仓库或同时修改同一文件。
- 主 Agent 负责整合、复核证据、运行最终验收、更新状态和提交；子 Agent 的结论未经复核不能作为完成证据。

## Git 规则

- Phase 开始先检查工作区，保留用户已有和无关修改；不得擅自覆盖、删除、重置或混入提交。
- 高风险修改开始前建立可识别、可回滚的 Git 基线。
- 开发中允许小而清晰的提交；正式 Phase 验收后必须有一个代表完成状态的提交。
- 提交信息格式为 `<type>(phase-<id>): <result>`，其中 `type` 使用 `feat`、`fix`、`refactor`、`docs`、`test` 或 `chore`。
- 提交前审查实际 diff，只暂存当前任务文件；测试或验收未通过时不得将阶段标记为完成。

## 禁止事项

除非当前 Phase 和必要 ADR 明确授权，不得随意更换核心框架、重构已验收系统、改变需求迁就实现、同时推进多个未验收 Phase，或为假想需求预建大量抽象。凭据和敏感值不得写入聊天、仓库文档、测试夹具或提交历史。
