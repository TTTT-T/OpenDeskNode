# Local toolchains and generated build output
.tools/
firmware/xiaozhi/build/
firmware/xiaozhi/managed_components/
firmware/xiaozhi/sdkconfig
firmware/xiaozhi/sdkconfig.old

# Phase evidence artifacts (large logs/wavs; hashes recorded in phase reports)
artifacts/

# Local configuration and secrets
.env
.env.*
!.env.example
data/.config.yaml
data/stock-gateway.sqlite3*
data/stock-gateway/

# macOS and editor state
.DS_Store
.vscode/settings.json

# Local bridge launcher (real LAN bind address; portable default stays in git)
scripts/run-bridge-local.sh
